---
import NewsPage, { getStaticPaths } from './[page].astro';

const paths = await getStaticPaths();
const firstPage = paths.find((p) => p.params.page === '1');
const props = firstPage?.props;
---

<NewsPage {...props} />