# AGENTS.md - AI開発運用ルール

このファイルは、東北ネヂRenewプロジェクトで Codex と Claude Code が共通して守る作業ルールです。

## 作業開始時

- 共通ルールとして、作業開始前に次を読む。
  1. `AGENTS.md`
  2. `PROJECT_CONTEXT.md`
- Claude Code のみ、必要に応じて `CLAUDE.md` を読む。
- 最初に `git status --short` を確認し、作業前の未追跡・変更ファイルを把握する。
- 調査だけを指示された場合は、ファイルの作成・編集・削除を行わない。

## 触れてはいけないファイル

明示的な指示がない限り、以下には触れない。

- `MEMO_運用備忘録.md`
- `MEMO_運用備忘録ブラウザメモ.md`
- `public/tools/`

以下は表示・編集・Git管理しない。

- `.env`
- `.env.*`
- APIキー、FTP情報、パスワード、トークンなどの秘密情報

秘密情報の存在確認が必要な場合も、値は出力しない。

## 変更方針

- 既存ファイルを推測で削除しない。
- ツール由来か人間作成か不明なファイルは、根拠を確認してから扱う。
- `src/pages` 配下は Astro の公開URLになるため、コピー、backup、hide、一時ファイルを置かない。
- `public` 配下はそのまま公開対象になり得るため、安全性を確認してから扱う。

## 作業順序

原則として、次の順で進める。

1. 調査
2. 根拠確認
3. 実装
4. Build
5. Git差分確認
6. 結果報告

## Git / Deploy

- Commit、Push、Deploy はユーザーの明示的な指示後にのみ行う。
- Commit、Push、Deploy を一括で勝手に進めない。
- Git add もユーザーの明示的な指示がある場合だけ行う。
- 本番デプロイ後は、対象URL、HTTP状態、表示内容を確認する。
